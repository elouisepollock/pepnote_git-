<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @item.name %>
</p>

<p>
<strong>Image:</strong>
<%= link_to(image_tag(@item.image.thumb.url, alt: 'Image'), @item.image.url, target: '_blank') if @item.image? %>
</p>

<%= link_to 'Edit', edit_item_path(@item) %> |
<%= link_to 'Back', items_path %>
